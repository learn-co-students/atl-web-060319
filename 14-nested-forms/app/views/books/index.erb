<h1>Books in the Library</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <a href="/books/<%= book.id %>"><%= book.author.full_name %> - <%= book.title %></a>
    </li>
  <% end %>
</ul>

<a href="/books/new">Add a new book</a>
<a href="/">Homepage</a>