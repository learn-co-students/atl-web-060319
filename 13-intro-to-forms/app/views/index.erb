<h1>Books in the Library</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <a href="/books/<%= book.id %>"><%= book.author %> - <%= book.title %></a>
    </li>
  <% end %>
</ul>

<a href="/">Homepage</a>